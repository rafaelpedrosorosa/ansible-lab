---
- name: exemplo2
  hosts: web
  tasks:
    - name: instalar diversos pacotes usando list
      ansible.builtin.yum:
        name:
          - nginx
          - postgresql
          - postgresql-server
        state: present

